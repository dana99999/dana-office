/** 환경 렌더러 — 바닥·벽·창문·가구·조명 (클라이언트 전용) */
import { at, COLS, ROWS, TS, W, H, WINDOWS, ZONES, inZone, inLounge, ENTRANCE } from "../world/map";
import type { ActorSnap } from "../world/types";

export interface DeskState { on: boolean; screen: string; mug: string }
export interface Ctx { t: number; hour: number; night: boolean; desk: Record<string, DeskState>; apiDown: boolean; doorOpen: boolean }

const FONT: Record<string, string[]> = { D: ["110", "101", "101", "101", "110"], A: ["010", "101", "111", "101", "101"], N: ["101", "111", "111", "101", "101"], O: ["111", "101", "101", "101", "111"], F: ["111", "100", "110", "100", "100"], I: ["111", "010", "010", "010", "111"], C: ["111", "100", "100", "100", "111"], E: ["111", "100", "110", "100", "111"], " ": ["000", "000", "000", "000", "000"] };
export function drawText(g: CanvasRenderingContext2D, x: number, y: number, str: string, c: string) { g.fillStyle = c; for (let i = 0; i < str.length; i++) { const f = FONT[str[i]] || FONT[" "]; for (let r = 0; r < 5; r++) for (let k = 0; k < 3; k++) if (f[r][k] === "1") g.fillRect(x + i * 4 + k, y + r, 1, 1); } }
export function skyFor(hour: number): [string, string] { if (hour >= 5 && hour < 7) return ["#5a4a7a", "#c98a6a"]; if (hour >= 7 && hour < 9) return ["#e8a26a", "#f4c98f"]; if (hour >= 9 && hour < 17) return ["#7fb6e8", "#c4e2f8"]; if (hour >= 17 && hour < 19) return ["#e07a5a", "#f2b27a"]; if (hour >= 19 && hour < 21) return ["#3e3f78", "#6a5a95"]; return ["#10152e", "#1d2550"]; }
export function nightAlpha(hour: number) { if (hour >= 8 && hour < 17) return 0; if (hour >= 17 && hour < 20) return (hour - 17) / 3 * 0.48; if (hour >= 20 || hour < 5) return 0.48; return 0.48 * (8 - hour) / 3; }

function floor(g: CanvasRenderingContext2D, x: number, y: number) {
  const X = x * TS, Y = y * TS, z = inZone(x, y);
  if (z) {
    g.fillStyle = z.rug[(x + y) % 2]; g.fillRect(X, Y, TS, TS); g.fillStyle = z.rug[(x + y + 1) % 2];
    for (let i = 0; i < 4; i++) { g.fillRect(X + 2 + i * 4, Y + 2 + (i % 2) * 8, 1, 1); g.fillRect(X + 4 + i * 4, Y + 12 - (i % 2) * 8, 1, 1); }
    g.fillStyle = z.inner; g.fillRect(X + ((x * 3 + y) % 8) + 4, Y + ((x + y * 3) % 8) + 4, 1, 1);
    g.fillStyle = z.border; if (x === z.x1) g.fillRect(X, Y, 1, TS); if (x === z.x2) g.fillRect(X + TS - 1, Y, 1, TS); if (y === z.y1) g.fillRect(X, Y, TS, 1); if (y === z.y2) g.fillRect(X, Y + TS - 1, TS, 1);
    g.fillStyle = z.inner; if (x === z.x1) g.fillRect(X + 2, Y, 1, TS); if (x === z.x2) g.fillRect(X + TS - 3, Y, 1, TS); if (y === z.y1) g.fillRect(X, Y + 2, TS, 1); if (y === z.y2) g.fillRect(X, Y + TS - 3, TS, 1);
  } else if (inLounge(x, y)) {
    g.fillStyle = y % 2 === 0 ? "#9a7452" : "#a37b58"; g.fillRect(X, Y, TS, TS); g.fillStyle = "#7d5c40"; g.fillRect(X, Y + 7, TS, 1); g.fillRect(X, Y + 15, TS, 1);
    g.fillRect(X + (x * 5 + y * 3) % 12, Y, 1, 7); g.fillRect(X + (x * 7 + y * 5) % 12, Y + 8, 1, 7); g.fillStyle = "#b08a64"; g.fillRect(X + 3, Y + 2, 4, 1); g.fillRect(X + 9, Y + 11, 3, 1);
    if ((x * y) % 5 === 1) { g.fillStyle = "#6b4c33"; g.fillRect(X + 10, Y + 4, 2, 1); g.fillRect(X + 9, Y + 5, 1, 1); }
  } else {
    g.fillStyle = (x + y) % 2 === 0 ? "#bab7c6" : "#b3b0bf"; if ((x * 7 + y * 3) % 11 === 0) g.fillStyle = "#aca9b9"; g.fillRect(X, Y, TS, TS);
    g.fillStyle = "#9e9bab"; g.fillRect(X, Y + TS - 1, TS, 1); g.fillRect(X + TS - 1, Y, 1, TS); g.fillStyle = "#c9c6d3"; g.fillRect(X, Y, TS - 1, 1); g.fillRect(X, Y, 1, TS - 1);
  }
}
function wall(g: CanvasRenderingContext2D, x: number, y: number, hour: number) {
  const X = x * TS, Y = y * TS;
  g.fillStyle = "#383b52"; g.fillRect(X, Y, TS, TS); g.fillStyle = "#33364c"; for (let i = 6; i < 14; i += 4) g.fillRect(X, Y + i, TS, 1);
  g.fillStyle = "#4c5070"; g.fillRect(X, Y, TS, 5); g.fillStyle = "#5c6188"; g.fillRect(X, Y, TS, 1); g.fillStyle = "#2a2c40"; g.fillRect(X, Y + TS - 2, TS, 2); g.fillStyle = "#4a4d66"; g.fillRect(X, Y + TS - 3, TS, 1);
  if (y === 0 && WINDOWS.includes(x)) {
    const sky = skyFor(hour); g.fillStyle = "#1e2033"; g.fillRect(X + 1, Y + 2, 14, 13); g.fillStyle = "#d9d6e6"; g.fillRect(X + 2, Y + 3, 12, 11);
    const grd = g.createLinearGradient(0, Y + 4, 0, Y + 13); grd.addColorStop(0, sky[0]); grd.addColorStop(1, sky[1]); g.fillStyle = grd; g.fillRect(X + 3, Y + 4, 10, 9);
    if (hour >= 20 || hour < 5) { g.fillStyle = "#fff"; g.fillRect(X + 5, Y + 6, 1, 1); g.fillRect(X + 10, Y + 8, 1, 1); g.fillRect(X + 8, Y + 5, 1, 1); g.fillStyle = "#f4f1d0"; g.fillRect(X + 11, Y + 5, 2, 2); }
    if (hour >= 9 && hour < 17) { g.fillStyle = "rgba(255,255,255,.55)"; g.fillRect(X + 4, Y + 5, 3, 1); g.fillRect(X + 5, Y + 6, 4, 1); }
    g.fillStyle = "#d9d6e6"; g.fillRect(X + 8, Y + 4, 1, 9); g.fillRect(X + 3, Y + 8, 10, 1); g.fillStyle = "#6a5f8c"; g.fillRect(X + 1, Y + 3, 1, 11); g.fillRect(X + 14, Y + 3, 1, 11); g.fillStyle = "#c9c6d3"; g.fillRect(X, Y + 14, TS, 1);
  }
}
export function drawChair(g: CanvasRenderingContext2D, x: number, y: number) { const X = x * TS, Y = y * TS; g.fillStyle = "#262838"; g.fillRect(X + 3, Y - 3, 10, 6); g.fillStyle = "#3b3f57"; g.fillRect(X + 4, Y - 2, 8, 4); g.fillStyle = "#4a4f6d"; g.fillRect(X + 5, Y - 1, 6, 1); g.fillStyle = "#262838"; g.fillRect(X + 1, Y + 5, 2, 5); g.fillRect(X + 13, Y + 5, 2, 5); g.fillRect(X + 2, Y + 11, 12, 3); g.fillStyle = "#3b3f57"; g.fillRect(X + 3, Y + 11, 10, 1); g.fillStyle = "#1c1e2c"; g.fillRect(X + 7, Y + 14, 2, 1); g.fillRect(X + 4, Y + 15, 8, 1); }
function desk(g: CanvasRenderingContext2D, x: number, y: number, c: Ctx) {
  const X = x * TS, Y = y * TS, st = c.desk[`${x},${y}`] || { on: false, screen: "doc", mug: "#4b44c4" };
  g.fillStyle = "#6b4a2f"; g.fillRect(X, Y + 3, TS, 11); g.fillStyle = "#8a6340"; g.fillRect(X, Y + 3, TS, 2); g.fillStyle = "#7a5636"; g.fillRect(X + ((x * 3) % 9) + 2, Y + 7, 5, 1); g.fillStyle = "#4a3220"; g.fillRect(X, Y + 14, TS, 1); g.fillStyle = "#3a2718"; g.fillRect(X + 1, Y + 15, 2, 1); g.fillRect(X + 13, Y + 15, 2, 1);
  g.fillStyle = "#f4f1ea"; g.fillRect(X, Y + 5, 3, 5); g.fillStyle = "#b9b6c4"; g.fillRect(X + 1, Y + 6, 1, 1); g.fillRect(X + 1, Y + 8, 1, 1); g.fillStyle = "#f2d54a"; g.fillRect(X + 1, Y + 11, 2, 2);
  g.fillStyle = "#3b3f57"; g.fillRect(X + 14, Y + 1, 1, 7); g.fillRect(X + 13, Y + 8, 3, 1); g.fillStyle = "#4a4f6d"; g.fillRect(X + 12, Y + 1, 3, 2); g.fillStyle = c.night && st.on ? "#ffd27a" : "#2b2e42"; g.fillRect(X + 12, Y + 3, 3, 1);
  g.fillStyle = "#262838"; g.fillRect(X + 3, Y - 1, 10, 9); g.fillRect(X + 7, Y + 8, 2, 1); g.fillRect(X + 5, Y + 9, 6, 1); g.fillStyle = st.on ? "#0f2a36" : "#141626"; g.fillRect(X + 4, Y, 8, 7);
  if (st.on) {
    const t = c.t;
    if (st.screen === "design") { const sw = ["#c9612b", "#2f7d6f", "#c9932c", "#8f2f5a"]; for (let i = 0; i < 4; i++) { g.fillStyle = sw[i]; g.fillRect(X + 4 + (i % 2) * 4, Y + (i < 2 ? 0 : 4), 3, 3); } g.fillStyle = "#8fd6e8"; g.fillRect(X + 4, Y + 3, 8, 1); }
    else if (st.screen === "growth") { const hs = [3, 5, 2, 6]; for (let j = 0; j < 4; j++) { const hh = Math.max(1, Math.round(hs[j] + Math.sin(t * 1.3 + j) * 1.5)); g.fillStyle = j % 2 ? "#4fc0bc" : "#8fd6e8"; g.fillRect(X + 4 + j * 2, Y + 7 - hh, 1, hh); } }
    else if (st.screen === "list") { g.fillStyle = "#8fd6e8"; for (let k = 0; k < 3; k++) g.fillRect(X + 4, Y + 1 + k * 2, 5 - (k % 2), 1); g.fillStyle = "#e8a33d"; g.fillRect(X + 10, Y + 1, 1, 1); }
    else if (st.screen === "grid") { g.fillStyle = "#8fd6e8"; for (let a = 0; a < 3; a++) for (let b = 0; b < 2; b++) g.fillRect(X + 4 + a * 3, Y + 1 + b * 3, 2, 2); }
    else { g.fillStyle = "#cfe9f2"; g.fillRect(X + 5, Y + 1, 6, 1); g.fillRect(X + 5, Y + 3, 4, 1); g.fillRect(X + 5, Y + 5, 5, 1); g.fillStyle = "#4b44c4"; g.fillRect(X + 5, Y + 3, 1, 1); }
  }
  g.fillStyle = "#3b3f57"; g.fillRect(X + 4, Y + 11, 7, 2); g.fillStyle = "#cfd2de"; for (let m = 0; m < 3; m++) g.fillRect(X + 5 + m * 2, Y + 11, 1, 1); g.fillRect(X + 12, Y + 11, 2, 2); g.fillStyle = "#9e9bab"; g.fillRect(X + 12, Y + 11, 1, 1);
  g.fillStyle = "#f4f1ea"; g.fillRect(X + 13, Y + 5, 2, 3); g.fillStyle = st.mug; g.fillRect(X + 13, Y + 6, 2, 1); g.fillStyle = "#f4f1ea"; g.fillRect(X + 15, Y + 6, 1, 1);
  if (st.on) { const ph = Math.floor(c.t * 2) % 3; g.fillStyle = "rgba(255,255,255,.55)"; g.fillRect(X + 13 + (ph % 2), Y + 3 - ph, 1, 1); g.fillRect(X + 14, Y + 2 - ((ph + 1) % 3), 1, 1); }
}
function bookshelf(g: CanvasRenderingContext2D, x: number, y: number) { const X = x * TS, Y = y * TS; g.fillStyle = "#5a3c24"; g.fillRect(X + 1, Y - 4, 14, 19); g.fillStyle = "#7a5434"; g.fillRect(X + 2, Y - 3, 12, 1); g.fillRect(X + 2, Y + 2, 12, 1); g.fillRect(X + 2, Y + 7, 12, 1); g.fillRect(X + 2, Y + 12, 12, 1); const books = ["#c0392b", "#2f7d6f", "#c9932c", "#4b44c4", "#8f2f5a", "#e8e8f0", "#1f4f7a"]; for (let s = 0; s < 3; s++) { const by = Y - 2 + s * 5; for (let i = 0; i < 6; i++) { g.fillStyle = books[(i + s * 2) % 7]; g.fillRect(X + 3 + i * 2, by + ((i + s) % 2), 1, 4 - ((i + s) % 2)); } } g.fillStyle = "#3f7a4d"; g.fillRect(X + 4, Y - 8, 6, 4); g.fillStyle = "#5aa66a"; g.fillRect(X + 5, Y - 9, 3, 2); g.fillStyle = "#8a5a3c"; g.fillRect(X + 5, Y - 5, 4, 2); g.fillStyle = "#2a2c40"; g.fillRect(X + 1, Y + 15, 14, 1); }
function couch(g: CanvasRenderingContext2D, x: number, y: number) { const X = x * TS, Y = y * TS, top = at(x, y - 1) !== "C"; g.fillStyle = "#3f4c8a"; g.fillRect(X + 1, Y + (top ? 1 : 0), 14, TS - (top ? 1 : 0)); g.fillStyle = "#5a68a8"; g.fillRect(X + 3, Y + 3, 10, 5); g.fillRect(X + 3, Y + 9, 10, 5); g.fillStyle = "#6f7cb8"; g.fillRect(X + 4, Y + 4, 3, 1); g.fillRect(X + 4, Y + 10, 3, 1); g.fillStyle = "#2e3868"; g.fillRect(X + 1, Y + (top ? 1 : 0), 2, TS - (top ? 1 : 0)); g.fillRect(X + 13, Y + (top ? 1 : 0), 2, TS - (top ? 1 : 0)); g.fillRect(X + 3, Y + 8, 10, 1); if (top) g.fillRect(X + 1, Y + 1, 14, 2); else { g.fillStyle = "#1c1e2c"; g.fillRect(X + 2, Y + 15, 2, 1); g.fillRect(X + 12, Y + 15, 2, 1); } }
function coffee(g: CanvasRenderingContext2D, x: number, y: number, c: Ctx) { const X = x * TS, Y = y * TS; g.fillStyle = "#6b4a2f"; g.fillRect(X, Y + 6, TS, 9); g.fillStyle = "#8a6340"; g.fillRect(X, Y + 6, TS, 1); g.fillStyle = "#4a3220"; g.fillRect(X, Y + 14, TS, 1); g.fillStyle = "#262838"; g.fillRect(X + 3, Y - 2, 8, 9); g.fillStyle = "#3b3f57"; g.fillRect(X + 4, Y - 1, 6, 2); g.fillStyle = "#e8a33d"; g.fillRect(X + 5, Y + 2, 1, 1); g.fillStyle = "#4fc0bc"; g.fillRect(X + 7, Y + 2, 1, 1); g.fillStyle = "#f4f1ea"; g.fillRect(X + 5, Y + 4, 3, 2); g.fillRect(X + 12, Y + 2, 3, 3); g.fillRect(X + 12, Y + 8, 3, 2); const ph = Math.floor(c.t * 2.5) % 3; g.fillStyle = "rgba(255,255,255,.5)"; g.fillRect(X + 6, Y + 2 - ph, 1, 1); }
function cooler(g: CanvasRenderingContext2D, x: number, y: number) { const X = x * TS, Y = y * TS; g.fillStyle = "#d9d6e6"; g.fillRect(X + 4, Y + 5, 8, 10); g.fillStyle = "#b9b6c4"; g.fillRect(X + 10, Y + 5, 2, 10); g.fillStyle = "#4b8fd6"; g.fillRect(X + 5, Y - 3, 6, 8); g.fillStyle = "#8fd6e8"; g.fillRect(X + 6, Y - 2, 2, 4); g.fillStyle = "#2a5f9e"; g.fillRect(X + 9, Y - 3, 2, 8); g.fillStyle = "#4fc0bc"; g.fillRect(X + 6, Y + 8, 1, 1); g.fillStyle = "#e9736a"; g.fillRect(X + 8, Y + 8, 1, 1); g.fillStyle = "#2a2c40"; g.fillRect(X + 4, Y + 15, 8, 1); }
function plant(g: CanvasRenderingContext2D, x: number, y: number, v: number) { const X = x * TS, Y = y * TS; if (v === 0) { g.fillStyle = "#8a5a3c"; g.fillRect(X + 5, Y + 9, 6, 6); g.fillStyle = "#a8714b"; g.fillRect(X + 5, Y + 9, 6, 1); g.fillStyle = "#6b4229"; g.fillRect(X + 5, Y + 14, 6, 1); g.fillStyle = "#3f7a4d"; g.fillRect(X + 3, Y + 2, 10, 7); g.fillStyle = "#5aa66a"; g.fillRect(X + 5, Y, 6, 4); g.fillRect(X + 2, Y + 4, 3, 2); g.fillRect(X + 11, Y + 3, 3, 2); g.fillStyle = "#2e5c3a"; g.fillRect(X + 9, Y + 6, 4, 3); g.fillRect(X + 3, Y + 7, 3, 2); g.fillStyle = "#7dc48c"; g.fillRect(X + 6, Y + 1, 2, 1); } else { g.fillStyle = "#d9d6e6"; g.fillRect(X + 5, Y + 8, 6, 7); g.fillStyle = "#b9b6c4"; g.fillRect(X + 9, Y + 8, 2, 7); g.fillStyle = "#3f7a4d"; g.fillRect(X + 7, Y + 1, 2, 7); g.fillRect(X + 4, Y + 3, 3, 2); g.fillRect(X + 9, Y + 4, 4, 2); g.fillRect(X + 5, Y + 6, 2, 2); g.fillRect(X + 10, Y + 7, 2, 1); g.fillStyle = "#5aa66a"; g.fillRect(X + 7, Y, 2, 2); g.fillRect(X + 3, Y + 2, 2, 2); g.fillRect(X + 12, Y + 3, 2, 2); } }
function server(g: CanvasRenderingContext2D, x: number, y: number, c: Ctx) { const X = x * TS, Y = y * TS, t = c.t; g.fillStyle = "#1e2033"; g.fillRect(X + 2, Y - 3, 12, 18); g.fillStyle = "#2f3247"; g.fillRect(X + 3, Y - 2, 10, 1); g.fillRect(X + 3, Y + 2, 10, 1); g.fillRect(X + 3, Y + 6, 10, 1); g.fillRect(X + 3, Y + 10, 10, 1); g.fillStyle = "#3b3f57"; for (let r = 0; r < 4; r++) g.fillRect(X + 8, Y - 1 + r * 4, 4, 2); g.fillStyle = Math.floor(t * 3) % 2 ? "#4fc0bc" : "#1f7a78"; g.fillRect(X + 4, Y - 1, 2, 1); g.fillRect(X + 4, Y + 7, 2, 1); g.fillStyle = Math.floor(t * 5) % 2 ? "#4fc0bc" : "#1f7a78"; g.fillRect(X + 4, Y + 3, 2, 1); g.fillStyle = c.apiDown ? "#e9736a" : "#e8a33d"; g.fillRect(X + 4, Y + 11, 2, 1); g.fillStyle = "#2a2c40"; g.fillRect(X + 2, Y + 15, 12, 1); g.fillStyle = "#1c1e2c"; g.fillRect(X + 14, Y + 8, 1, 7); }
function booth(g: CanvasRenderingContext2D, x: number, y: number) { const X = x * TS, Y = y * TS; g.fillStyle = "#2b2e42"; g.fillRect(X + 1, Y - 4, 14, 19); g.fillStyle = "#3b3f57"; g.fillRect(X + 2, Y - 3, 12, 1); g.fillStyle = "rgba(143,214,232,.5)"; g.fillRect(X + 3, Y - 1, 10, 10); g.fillStyle = "rgba(255,255,255,.35)"; g.fillRect(X + 4, Y, 1, 8); g.fillRect(X + 5, Y, 1, 3); g.fillStyle = "#3b3f57"; g.fillRect(X + 3, Y + 9, 10, 1); g.fillStyle = "#e8a33d"; g.fillRect(X + 12, Y + 4, 1, 1); g.fillStyle = "#5c6188"; g.fillRect(X + 5, Y - 3, 6, 1); g.fillStyle = "#e9736a"; g.fillRect(X + 7, Y - 3, 2, 1); }
export function reception(g: CanvasRenderingContext2D, x: number, y: number) { const X = x * TS - 8, Y = y * TS; g.fillStyle = "#3b34a8"; g.fillRect(X, Y + 2, 32, 11); g.fillStyle = "#4b44c4"; g.fillRect(X, Y + 2, 32, 3); g.fillStyle = "#5a54d8"; g.fillRect(X, Y + 2, 32, 1); g.fillStyle = "#2a2580"; g.fillRect(X, Y + 12, 32, 1); g.fillStyle = "#1f1b60"; g.fillRect(X, Y + 13, 32, 1); drawText(g, X + 3, Y + 6, "DANA", "#f4f1ea"); g.fillStyle = "#f4f1ea"; g.fillRect(X + 22, Y + 6, 7, 5); g.fillStyle = "#e8a33d"; g.fillRect(X + 23, Y + 7, 2, 3); g.fillStyle = "#3b34a8"; g.fillRect(X + 26, Y + 7, 1, 1); g.fillRect(X + 26, Y + 9, 1, 1); g.fillStyle = "#e8a33d"; g.fillRect(X + 29, Y + 3, 2, 2); g.fillStyle = "#3f7a4d"; g.fillRect(X - 1, Y - 1, 4, 4); g.fillStyle = "#8a5a3c"; g.fillRect(X, Y + 3, 2, 1); }
function door(g: CanvasRenderingContext2D, x: number, y: number, open: boolean) { const X = x * TS, Y = y * TS; g.fillStyle = "#2a2c40"; g.fillRect(X, Y, TS, TS); g.fillStyle = "#5c6188"; g.fillRect(X, Y, TS, 1); g.fillStyle = "#4c5070"; g.fillRect(X + 1, Y + 1, 14, 2); if (open) { g.fillStyle = "#bab7c6"; g.fillRect(X + 3, Y + 3, 10, 12); g.fillStyle = "#7a5434"; g.fillRect(X + 1, Y + 3, 2, 12); g.fillStyle = "#5a3c24"; g.fillRect(X + 1, Y + 3, 1, 12); } else { g.fillStyle = "#7a5434"; g.fillRect(X + 3, Y + 3, 10, 12); g.fillStyle = "#5a3c24"; g.fillRect(X + 4, Y + 4, 8, 10); g.fillStyle = "#7a5434"; g.fillRect(X + 5, Y + 5, 6, 4); g.fillStyle = "#e8a33d"; g.fillRect(X + 10, Y + 9, 1, 1); } g.fillStyle = "#4fc0bc"; g.fillRect(X + 6, Y + 1, 4, 1); }
function table(g: CanvasRenderingContext2D, x: number, y: number) { const X = x * TS, Y = y * TS, top = at(x, y - 1) !== "T", bot = at(x, y + 1) !== "T", lef = at(x - 1, y) !== "T", rig = at(x + 1, y) !== "T"; g.fillStyle = "#8a6a44"; g.fillRect(X, Y, TS, TS); if (top) { g.fillStyle = "#a58255"; g.fillRect(X, Y + 1, TS, 2); g.fillStyle = "#6e5436"; g.fillRect(X, Y, TS, 1); } if (bot) { g.fillStyle = "#5e4528"; g.fillRect(X, Y + TS - 2, TS, 2); } if (lef) { g.fillStyle = "#6e5436"; g.fillRect(X, Y, 1, TS); } if (rig) { g.fillStyle = "#6e5436"; g.fillRect(X + TS - 1, Y, 1, TS); } if (x === 18 && y === 8) { g.fillStyle = "#262838"; g.fillRect(X + 3, Y + 4, 10, 7); g.fillStyle = "#8fd6e8"; g.fillRect(X + 4, Y + 5, 8, 4); g.fillStyle = "#3b3f57"; g.fillRect(X + 4, Y + 11, 8, 1); } if (x === 17 && y === 9) { g.fillStyle = "#f4f1ea"; g.fillRect(X + 4, Y + 5, 7, 6); g.fillStyle = "#9e9bab"; g.fillRect(X + 5, Y + 6, 5, 1); g.fillRect(X + 5, Y + 8, 4, 1); } if (x === 19 && y === 9) { g.fillStyle = "#f4f1ea"; g.fillRect(X + 5, Y + 6, 2, 3); g.fillStyle = "#4b44c4"; g.fillRect(X + 5, Y + 7, 2, 1); g.fillStyle = "#f4f1ea"; g.fillRect(X + 7, Y + 7, 1, 1); } }
function bin(g: CanvasRenderingContext2D, x: number, y: number) { const X = x * TS, Y = y * TS; g.fillStyle = "#6e7387"; g.fillRect(X + 5, Y + 6, 6, 8); g.fillStyle = "#8a8fa3"; g.fillRect(X + 5, Y + 6, 6, 1); g.fillStyle = "#4f5468"; g.fillRect(X + 9, Y + 7, 2, 7); g.fillStyle = "#f4f1ea"; g.fillRect(X + 6, Y + 5, 3, 1); }

/** 정적 배경 (시간대별 1회 렌더) */
export function buildBackground(bg: HTMLCanvasElement, hour: number) {
  const g = bg.getContext("2d")!; g.imageSmoothingEnabled = false; g.fillStyle = "#0b0c14"; g.fillRect(0, 0, W, H);
  for (let y = 0; y < ROWS; y++) for (let x = 0; x < COLS; x++) if (at(x, y) !== "#") floor(g, x, y);
  g.fillStyle = "#4f4470"; g.fillRect(16 * TS + 3, 7 * TS + 8, 5 * TS - 6, 4 * TS - 2); g.fillStyle = "#5d5282"; g.fillRect(16 * TS + 5, 7 * TS + 10, 5 * TS - 10, 4 * TS - 6); g.fillStyle = "#4f4470"; for (let i = 0; i < 6; i++) { g.fillRect(16 * TS + 7 + i * 12, 7 * TS + 12, 4, 1); g.fillRect(16 * TS + 7 + i * 12, 11 * TS + 2, 4, 1); }
  if (hour >= 8 && hour < 17) for (const wx of WINDOWS) { g.fillStyle = "rgba(255,250,220,.10)"; g.fillRect(wx * TS + 1, TS, 14, TS * 2); g.fillStyle = "rgba(255,250,220,.05)"; g.fillRect(wx * TS - 1, TS * 3, 18, TS); }
  for (let y = 0; y < ROWS; y++) for (let x = 0; x < COLS; x++) if (at(x, y) === "#") wall(g, x, y, hour);
  g.fillStyle = "#f4f1ea"; g.fillRect(2 * TS + 2, 4, 28, 10); g.fillStyle = "#9e9bab"; g.fillRect(2 * TS + 4, 6, 10, 1); g.fillRect(2 * TS + 4, 9, 18, 1); g.fillRect(2 * TS + 4, 12, 8, 1); g.fillStyle = "#4b44c4"; g.fillRect(2 * TS + 18, 6, 6, 2); g.fillStyle = "#c0392b"; g.fillRect(2 * TS + 26, 11, 2, 2);
  const arts = ["#c9612b", "#2f7d6f", "#c9932c", "#8f2f5a", "#4b44c4", "#1f4f7a"]; g.fillStyle = "#3b2f24"; g.fillRect(8 * TS + 1, 3, 62, 12);
  for (let a = 0; a < 6; a++) { g.fillStyle = "#f4f1ea"; g.fillRect(8 * TS + 3 + a * 10, 5, 8, 8); g.fillStyle = arts[a]; g.fillRect(8 * TS + 4 + a * 10, 6, 6, 5); g.fillStyle = "#e8a33d"; g.fillRect(8 * TS + 6 + a * 10, 4, 2, 1); }
  g.fillStyle = "#1f2233"; g.fillRect(17 * TS + 2, 4, 28, 10); g.fillStyle = "#4fc0bc"; g.fillRect(17 * TS + 4, 6, 3, 6); g.fillRect(17 * TS + 8, 8, 3, 4); g.fillRect(17 * TS + 12, 7, 3, 5); g.fillRect(17 * TS + 16, 10, 3, 2); g.fillStyle = "#e8a33d"; g.fillRect(17 * TS + 20, 5, 6, 1); g.fillRect(17 * TS + 20, 7, 4, 1);
  g.fillStyle = "#f4f1ea"; g.fillRect(6 * TS + 4, 4, 8, 8); g.fillStyle = "#262838"; g.fillRect(6 * TS + 3, 5, 1, 6); g.fillRect(6 * TS + 12, 5, 1, 6); g.fillRect(6 * TS + 5, 3, 6, 1); g.fillRect(6 * TS + 5, 12, 6, 1);
  for (let f = 0; f < 3; f++) { const fy = (7 + f) * TS; g.fillStyle = "#3b2f24"; g.fillRect(6 * TS + 2, fy + 3, 12, 10); g.fillStyle = "#f4f1ea"; g.fillRect(6 * TS + 3, fy + 4, 10, 8); g.fillStyle = arts[(f + 2) % 6]; g.fillRect(6 * TS + 4, fy + 5, 8, 6); g.fillStyle = "#fff"; g.fillRect(6 * TS + 5, fy + 6, 2, 1); }
  g.fillStyle = "#1f2233"; g.fillRect(15 * TS + 2, 8 * TS + 2, 12, 10);
  drawText(g, 9 * TS + 2, 12 * TS + 5, "DANA OFFICE", "#8fd6e8"); g.fillStyle = "#e8a33d"; g.fillRect(9 * TS - 4, 12 * TS + 5, 3, 5);
  for (let y = 0; y < ROWS; y++) for (let x = 0; x < COLS; x++) { const c = at(x, y); if (c === "B") bookshelf(g, x, y); else if (c === "C") couch(g, x, y); else if (c === "W") cooler(g, x, y); else if (c === "P") plant(g, x, y, (x + y) % 2); else if (c === "O") booth(g, x, y); else if (c === "T") table(g, x, y); }
  bin(g, 1, 4); bin(g, 14, 4); bin(g, 14, 10);
  g.fillStyle = "#6b4a2f"; g.fillRect(1 * TS + 4, 10 * TS + 6, 8, 6); g.fillStyle = "#3b3f57"; g.fillRect(1 * TS + 7, 10 * TS + 1, 2, 5); g.fillStyle = "#e8a33d"; g.fillRect(1 * TS + 5, 10 * TS, 6, 2);
  plant(g, 20, 7, 1);
}
/** 동적 가구 (매 프레임) */
export function drawDynamic(g: CanvasRenderingContext2D, c: Ctx, simMin: number) {
  for (let y = 0; y < ROWS; y++) for (let x = 0; x < COLS; x++) { const ch = at(x, y); if (ch === "D") desk(g, x, y, c); else if (ch === "K") coffee(g, x, y, c); else if (ch === "S") server(g, x, y, c); else if (ch === "E") door(g, x, y, c.doorOpen); }
  reception(g, 10, 6);
  for (let b = 0; b < 4; b++) { const hh = Math.max(1, Math.round(4 + Math.sin(c.t * 0.9 + b * 1.3) * 2.5)); g.fillStyle = b % 2 ? "#4fc0bc" : "#8fd6e8"; g.fillRect(15 * TS + 4 + b * 3, 8 * TS + 10 - hh, 2, hh); }
  const ang = ((simMin / 60) % 12) / 12 * Math.PI * 2, mang = (simMin % 60) / 60 * Math.PI * 2, cx = 6 * TS + 8, cy = 8;
  g.fillStyle = "#262838"; g.fillRect(Math.round(cx + Math.sin(ang) * 2), Math.round(cy - Math.cos(ang) * 2), 1, 1); g.fillRect(Math.round(cx + Math.sin(mang) * 3), Math.round(cy - Math.cos(mang) * 3), 1, 1); g.fillRect(cx, cy, 1, 1);
}
export function drawLight(g: CanvasRenderingContext2D, hour: number, lamps: [number, number][], monitors: [number, number][], zonesLit: { x1: number; y1: number; x2: number; y2: number }[]) {
  const a = nightAlpha(hour); if (a <= 0) return;
  g.fillStyle = hour >= 4 && hour < 8 ? "#2a2148" : "#0b1030"; g.globalAlpha = a; g.fillRect(0, 0, W, H); g.globalAlpha = 1; g.globalCompositeOperation = "lighter";
  for (const z of zonesLit) { const cx = (z.x1 + z.x2 + 1) / 2 * TS, cy = (z.y1 + z.y2 + 1) / 2 * TS; const grd = g.createRadialGradient(cx, cy, 4, cx, cy, 64); grd.addColorStop(0, "rgba(255,235,190,.16)"); grd.addColorStop(1, "rgba(255,235,190,0)"); g.fillStyle = grd; g.fillRect(cx - 70, cy - 70, 140, 140); }
  for (const t of lamps) { const cx = t[0] * TS + 13, cy = t[1] * TS + 18; const grd = g.createRadialGradient(cx, cy, 1, cx, cy, 26); grd.addColorStop(0, "rgba(255,200,110,.5)"); grd.addColorStop(1, "rgba(255,200,110,0)"); g.fillStyle = grd; g.fillRect(cx - 28, cy - 28, 56, 56); }
  for (const t of monitors) { const cx = t[0] * TS + 8, cy = t[1] * TS + 3; const grd = g.createRadialGradient(cx, cy, 1, cx, cy, 13); grd.addColorStop(0, "rgba(143,214,232,.4)"); grd.addColorStop(1, "rgba(143,214,232,0)"); g.fillStyle = grd; g.fillRect(cx - 14, cy - 14, 28, 28); }
  for (const wx of WINDOWS) { const grd = g.createLinearGradient(0, TS, 0, TS * 3); grd.addColorStop(0, "rgba(170,190,255,.14)"); grd.addColorStop(1, "rgba(170,190,255,0)"); g.fillStyle = grd; g.fillRect(wx * TS + 1, TS, 14, TS * 2); }
  g.globalCompositeOperation = "source-over";
}
export function deskStatesFor(actors: ActorSnap[]): { desk: Record<string, DeskState>; lamps: [number, number][]; mons: [number, number][]; zonesLit: typeof ZONES } {
  const desk: Record<string, DeskState> = {}, lamps: [number, number][] = [], mons: [number, number][] = [], zonesLit: typeof ZONES = []; const seen = new Set<string>();
  for (const a of actors) { const d: [number, number] = [a.seat[0], a.seat[1] + 1]; const working = a.present && (a.kind === "human" || (a.state === "work" && a.queue > 0)) && !a.moving && a.x === a.seat[0] && a.y === a.seat[1]; if (at(d[0], d[1]) === "D") desk[`${d[0]},${d[1]}`] = { on: working, screen: a.screen, mug: a.look.top[0] }; if (working) { lamps.push(d); mons.push(d); const z = inZone(a.x, a.y); if (z && !seen.has(z.key)) { seen.add(z.key); zonesLit.push(z); } } }
  return { desk, lamps, mons, zonesLit };
}
export { ENTRANCE };
